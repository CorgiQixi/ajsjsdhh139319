name = "ajsjsdhh139319"
main = "worker.js"
compatibility_date = "2024-03-20"

[durable_objects]
bindings = [
  { name = "CHAT_SESSION", class_name = "ChatSession" }
]

# 关键修改：将 new_classes 改为 new_sqlite_classes
[[migrations]]
tag = "v1"
new_sqlite_classes = ["ChatSession"]

[vars]
USER_API_URL = "https://chatlogin.qixibest.workers.dev/list-users"
